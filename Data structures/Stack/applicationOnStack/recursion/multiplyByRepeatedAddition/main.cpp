//multiply by repeated addition
#include "multiplyByRepeatedAddition.cpp"

int multiply(int ,int );

int main()
{
    int x, y;
    cout << "Enter two numbers: "; cin >> x >> y;
    cout << "result = " << multiply(x,y);
    return 0;
}
